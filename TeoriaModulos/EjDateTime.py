from datetime import datetime
from datetime import timedelta
import time
print(datetime.now())
delta = timedelta(days=50, seconds=27,  microseconds=10,   milliseconds=29000,   minutes=5,    hours=8,    weeks=2)
import calendar
año=2022
mes=2
cm=calendar.month(año,mes)
print(cm)
